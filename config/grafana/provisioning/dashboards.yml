# Grafana 仪表盘自动加载配置
apiVersion: 1

providers:
  - name: 'Quant Dashboards'
    orgId: 1
    folder: 'Quant'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
