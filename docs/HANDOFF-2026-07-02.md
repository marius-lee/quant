# Handoff — A股量化选股系统
**日期**: 2026-07-02 20:00 | **分支**: main | **环境**: Python 3.14, akshare 1.18.64

---

## 项目概述

北极星量化选股系统。7 层 Pipeline：Data → Load → Factor+Alpha → Risk → Optimizer → Execution → Monitor。
本金 ¥5000 起，全仓 1 只股票，初始资金随盈利增长自动调仓。

---

## 本次会话修复记录 (按时间倒序)

### 4th batch: turnover + cache refresh
- `24d70ae` — `turnover_rev_5d` 退化为 volume fallback（turnover<50 stocks 时）
- `b89511f` — `_fetch_value_em` 加进度日志 + sleep 500→200ms

### 3rd batch: fundamental.py API 迁移
- `f210555` — 用 `stock_value_em` 替代已移除的 `stock_a_lg_indicator` / `stock_a_indicator_lg`
- `6bf5d2b` — P2 文档化

### 2nd batch: P1 IC 自适应
- `6872020` — 移除 pipeline.py 硬编码 ic_map，改从 `factor_cache.json` 动态读取

### 1st batch: P0 数据+止损+持仓
- `dbe333b` — adjust=""→"qfq", stop_loss_pct 统一, engine pos_value fix

---

## 因子状态

**8 个有效因子** (IC > 0.01, 自动参与 alpha 合成):
reversal_5d (0.067), momentum_10d (0.058), range_20d (0.048), volatility_20d (0.042),
idio_vol_20d (0.042), max_ret_20d (0.039), gap_5d (0.026), skewness_20d (0.024)

**7 个无效因子** (IC≈0, 自动排除):
amihud_20d, turnover_rev_5d, hsgt_flow_5d, ep_ratio, bp_ratio, roe_ratio, high52w_dist

IC 权重从 `data/factor_cache.json` 加载，`load_ic_map_from_cache()` 过滤 IC<0.01。

---

## 当前待办

1. **终端 500 只 PE/PB 同步** — 用 `stock_value_em` 逐只拉取，进行中
2. **同步完成后刷新 factor_cache**:
   ```bash
   cd /Users/mariusto/project/quant
   PYTHONPATH=. .venv/bin/python3 -c "
   from factor.stats_cache import force_refresh_cache
   force_refresh_cache(n_symbols=500)
   "
   ```
3. **P0-1 数据复权生效** — 代码已改 `adjust="qfq"`，但已有 16M 行 daily 数据是未复权的。下次拉数据才生效。如需重建：删除 daily 表重新 sync_all()
4. **northbound** — `stock_hsgt_individual_em` 沙箱 DNS 阻断，本机可跑

---

## 关键架构决策

- **本金动态增长**: 初始 ¥5000，盈利后 `total_capital` 自动增加，调仓金额随之扩大
- **全仓 1 只**: `max_positions=1`, `max_single_position=1.00`
- **止损**: 日频检查，阈值 15%（`risk.stop_loss_pct: 0.15`）
- **复权**: tencent 源用 qfq，akshare 源已改为 qfq
- **北交所排除**: 50 万保证金未开通，SQL 层 `market!='BJ'`
- **akshare 1.18.64**: 旧 API `stock_a_lg_indicator` / `stock_a_indicator_lg` 已移除

---

## 数据源 (akshare 1.18.64)

| 功能 | API | 状态 |
|------|-----|------|
| 日线 | `stock_zh_a_hist(adjust="qfq")` | ✓ 已改 |
| 日线 | tencent `qfqday` | ✓ 一直是 |
| PE/PB/市值 | `stock_value_em(symbol)` 逐只 | ✓ 新方案 |
| 行业 | `stock_individual_info_em` 逐只 | ✓ |
| 北向 | `stock_hsgt_individual_em` | ✗ DNS阻断 |
| 基准 | `stock_zh_index_daily_em("sh000300")` | ✓ |

---

## 沙箱限制

- `datacenter-web.eastmoney.com` / `money.finance.sina.com.cn` / `www.legulegu.com` DNS 全部阻断
- 所有 akshare API 需本机终端运行
- 代码逻辑正确，仅运行环境限制

---

## 用户偏好

- 小资金全仓 1 只，不分散
- 本金动态增长
- Token 消耗关注：减少冗余输出 (A)，必要时 handoff 重置 (C)
- 代码修改逐批 git commit，历史可回溯
- 专业量化体系，非拍脑袋策略

---

## 关键文件索引

| 文件 | 职责 |
|------|------|
| `pipeline.py` | 7 层 Pipeline 主控 |
| `backtest.py` | 回测 + 日频止损 |
| `data/store.py` | SQLite 数据仓库 |
| `data/fundamental.py` | PE/PB/ROE 同步 (stock_value_em) |
| `data/northbound.py` | 北向资金 (需本机) |
| `data/trade_repo.py` | 模拟交易记录 |
| `factor/compute.py` | 因子计算函数 |
| `factor/synth.py` | 因子合成 (equal_weight / ic_weighted) |
| `factor/evaluate.py` | IC/IR/衰减/相关性 |
| `factor/stats_cache.py` | 因子缓存 + IC 加载 + 刷新 |
| `alpha/model.py` | AlphaModel (calibrate + predict) |
| `execution/engine.py` | 模拟执行引擎 |
| `optimizer/rebalance.py` | 调仓计算 |
| `risk/constraints.py` | 风控过滤 |
| `config/config.yaml` | 集中配置 |
| `web/app.py` | Flask 前端 |
| `docs/analysis-2026-07-02.md` | 全盘分析文档 |
| `docs/HANDOFF-2026-07-02.md` | 本文件 |
