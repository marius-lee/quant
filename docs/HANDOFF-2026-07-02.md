# Handoff — A股量化选股系统

> **⚠️ ARCHIVED** — 此文件为历史快照，仅供追溯参考。当前状态见 [HANDOFF.md](/Users/mariusto/project/quant/docs/HANDOFF.md)。

**Date**: 2026-07-02 22:45 | **Project**: `/Users/mariusto/project/quant`

---

## 当前运行状态

**PE/PB 全量同步** — `data/fundamental.py` `sync_all(conn, max_fetch=-1)` — ~5525 stocks via `stock_value_em`。当前进度 ~396/5525，预计还需 20-30min。

同步完成后执行：
```bash
cd /Users/mariusto/project/quant
PYTHONPATH=. .venv/bin/python3 -c "
from factor.stats_cache import force_refresh_cache
force_refresh_cache(n_symbols=500)
"
```

---

## 本次会话修复清单

### P0 (崩溃级)
| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 1 | `backtest.py:34` | IndentationError — import 缩进丢失 | ✅ fixed |
| 2 | `backtest.py:139` | 变量 `i` 未定义 (NameError) | ✅ fixed |
| 3 | `factor/compute.py:203` | `compute_amihud` 中 `results` 未定义 | ✅ fixed |
| 4 | `factor/stats_cache.py:299` | n_symbols 硬编码 200，上层传参被忽略 | ✅ fixed |

### P1 (可运行但影响正确性)
| # | 文件 | 问题 | 状态 |
|---|------|------|------|
| 5 | `data/fundamental.py:114` | 重复 `print()` | ✅ fixed |
| 6 | `backtest.py` | `pipeline.cfg(...)` 脆弱耦合 | ✅ fixed |
| 7 | `data/store.py:564` | `_analyze_daily_gaps` 不含 BJ 过滤 | ✅ fixed |
| 8 | `factor/compute.py` | bp_ratio/skewness 方向注释不准 | ✅ fixed |
| 9 | `config/config.yaml` | stop_loss 注释 5%→15% | ✅ fixed |

### 日志覆盖 (新加)
| 模块 | 修复前 | 修复后 |
|------|--------|--------|
| `data/trade_repo.py` | 0 条 | 4 条 (每笔交易写入) |
| `monitor/report.py` | 0 条 | 4 条 (报告生成) |
| `risk/neutralize.py` | 0 条 | 3 条 (中性化标记) |
| `optimizer/portfolio.py` | 0 条 | 4 条 (资金档位) |
| `execution/cost.py` | 0 条 | 3 条 (成本明细) |
| `monitor/attribution.py` | 0 条 | 3 条 (Brinson 归因) |

### M6 确认已修复
- 前端 Factor Tab 已接入真实 `get_cached_factor_stats()`，demo 假数据已移除

---

## Git 提交历史 (本次)
```
72b3971 fix(P0): backtest SyntaxError + compute_amihud NameError + stats_cache n_symbols
5090cd6 fix(P1): config import + BJ filter + factor direction comments
8ed16a2 feat(logging): add strategic logging to 6 silent modules
```

---

## 后续待办

1. **PE/PB sync 完成** → `force_refresh_cache(500)` → 检查 roe_ratio/high52w_dist IC
2. **northbound sync** — 需本机终端 (沙箱 DNS 阻断)
3. **P0-1: daily 数据复权** — 代码已 fix `adjust="qfq"`，但已有 16M 行数据未复权
4. **回测全链路测试** — sync 结束后跑 `python backtest.py 2026-01-01 2026-06-30 5000`

## 架构关键决策
- 本金动态增长: total_capital = cash + positions_value
- 全仓 1 只: max_positions=1, max_single_position=1.00
- 止损: 15% 日频检查
- 北交所排除: SQL 层 market!='BJ'
- akshare 1.18.64: 旧 API 已迁移到 stock_value_em
