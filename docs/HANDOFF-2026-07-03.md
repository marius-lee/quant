
# Handoff — A股量化选股系统

**最后更新**: 2026-07-03 17:50 CST
**当前状态**: 5因子回测 +80.5% (Sharpe 2.10). Phase 8 完成 — margin 因子不成立(SSE+SZSE IC=0.004). 数据已入库(478K行).
**Git**: main @ latest

---

## 协作规则 (每次会话必须遵守)

| # | 规则 |
|---|------|
| R1 | 执行前逐行脑内验代码 |
| R2 | apply_patch 失败一次就换 Python 脚本 |
| R3 | 数据库查询/sync 命令给用户在终端跑 |
| R4 | 因子加减必须更新 FACTOR-AUDIT + HANDOFF |
| R5 | 每完成一个阶段 git commit + 更新 HANDOFF |
| R6 | 代码修改先读完整文件, 不做局部猜测 |
| R7 | 禁止代码残留 (if True: 等) |
| R8 | 禁止浪费 token — 终端操作用户跑 |
| R9 | compact context 后先读 HANDOFF |
| R10 | 数据库操作写脚本给用户跑 |
| **R11** | **因子弃用前置条件: 同时满足 回测不降 + 相关性冗余 + 归档记录. IC排名≠因子价值** |

---

## 当前活跃因子 (Phase 10 最终: 5-Factor Model)

| 因子 | IC | IR | 维度 | 来源 |
|------|-----|-----|------|------|
| bp_ratio | +0.062 | +0.26 | 价值 | Fama-French 1992 |
| zt_streak | +0.055 | +0.75 | 涨停连板 | A股独有 (daily OHLCV自算) |
| size | +0.044 | +0.33 | 规模 | Fama-French 1993 |
| gap_5d | +0.043 | +0.36 | 隔夜缺口 | A股T+1异象 |
| amihud_20d | +0.025 | +0.24 | 流动性 | Amihud 2002 (回测证实不可替代) |

**回测验证**: 5因子 +80.5%, Sharpe 2.10, α +75.0% (2026-01-01~2026-06-30, ¥5,000→¥9,027)

**Phase 10 结论**: 3新因子(high52w_dist, dt_streak, vol_price_corr_10d)全量审计IC优秀但回测全部低于基线. 回归5因子.

**上次更新**: 2026-07-03 20:56 CST | Git: main @ Phase 10 final

---

## 数据库状态

| 表 | 行数 | 说明 |
|----|------|------|
| daily | 16.5M | qfq前复权, 2020-2026 |
| stocks | 5,525 | 含317北交所 (已排除) |
| northbound_flow | 579K | 466只, 截止2024-08 (API硬截断) |
| lhb_detail | 25K | 4,292只, 2025-01~2026-07 |
| limit_up_pool | 1,249 | 12天 (API仅保留~1月) |
| margin_detail | 478,632 | SSE+SZSE, 118交易日, 2026-01~07 |
| fund_flow | 0 | IP被封, 待解封 |

---

## Phase 7 完成记录

- lhb_post_quality: IC=0.013 → 弃用
- dt_streak: IC=0.037 但替换 amihud 回测 +41.8% → 弃用
- 核心教训: 协方差结构 > 个体 IC

## Phase 8 完成记录 (2026-07-03)

- margin_detail SSE sync: ✅ 45,555 行/23 天 (JSON API 直接调用)
- margin_detail SZSE: ❌ API 返回空 JSON
- fund_flow: ❌ IP 被东方财富封
- margin_buy_ratio: IC=**+0.090** (最强), 但仅 23/116 天覆盖 → 暂弃用, 等数据积累
- margin_balance_chg: IC=+0.004 → 弃用

---



## Phase 10 最终结论 (2026-07-03 20:54)

- 30因子全量审计 → 3候选 → 逐步回测 → 全部弃用 → 回归5因子
- 核心教训: IC排名≠回测有效 (high52w_dist IC=+0.095但回测+12.1%)
- 修复: abs(IC)方向bug (stats_cache.py + synth.py)
- 5因子 = 当前数据/方法约束下的最优解

## 技术债

- `factor/compute.py` 有历史修补残留, 建议重构
- SZSE 融资融券 API 待修复
- fund_flow IP 解封时间未知
- 3 个数据库 (market.db/trades.db/benchmark.db) 待合并

---

## Phase 8 最终结论 (2026-07-03)

margin_buy_ratio 经历完整测试:
- **look-ahead bias**: 修复前 IC=+0.090 (虚高), 修复后 IC=+0.043 (SSE only)
- **全量稀释**: SSE+SZSE IC=+0.004 → 不成立
- **根因**: 上交所和深交所融资动态不同, 强行合并消去信号

**最终因子阵容**: bp_ratio + size + gap_5d + zt_streak + amihud_20d = +80.5%

**数据资产**: margin_detail 478K行已入库, 可用于未来按市场拆分的因子探索.

## 2026-07-04 会话追加

### 主要改动

1. **`data/store.py`**: `get_financials()` 重写
   - 修复 cash_flow 合并 bug（pandas MergeError 被 `except: pass` 静默吞掉）
   - 4 个新财务因子现已正常工作

2. **因子评估标准重构** (ADR 007)
   - 取消固定 IC 阈值 (0.01/0.02)
   - 采用 Grinold & Kahn (1999) + Harvey, Liu & Zhu (2016) 三层框架
   - 新增 `factor/marginal.py`: 统计显著性 t-test + 边际 IC 计算
   - 新评估入口: `scripts/eval_stepwise.sh`

3. **构建脚本幂等化**: `scripts/build_fin_factors.sh` Step 2/3 加重复插入检查

### 待评估

4 个新财务因子（roe_reported, roa, debt_ratio, accruals）IC 已计算，但需通过三层检验才能激活。
运行 `bash scripts/eval_stepwise.sh` 进行完整评估。

## 2026-07-04 会话结束状态

### 立即可执行

```bash
bash scripts/eval_stepwise.sh
```

三层评估：统计显著性 (t≥2.0) → 边际贡献 (Grinold & Kahn) → 步进回测 (IR 不降就留)。

### 关键修复

- `data/store.py`: get_financials() cash_flow 合并 bug 修复
- `factor/marginal.py`: 边际贡献评估，所有无依据参数已移除
- `factor/stats_cache.py`: `_load_ic_from_db` 移除 0.01 IC 过滤
- `scripts/eval_stepwise.sh`: 三层评估入口，`0.005` ε 和 `5%` 容忍度已移除

### 参数审计结果

已移除所有无依据参数（0.01, 0.03, 0.005, 5%）。
保留的参数均有业界依据并已在 ADR 007 中记录。

### 已提交

- `ed20b99` P11: 因子评估标准重构
- `b6ffc63` P11 followup: 参数审计修复

### Skills

- `~/.codex/skills/coding-standards/SKILL.md` 已安装
- 下次会话自动激活，10 个模板约束所有代码生成
