# Phase 1.3 完成归档 — TickFlowSource 完善

## 变更摘要
为 `TickFlowSource` 补充 5 个缺失操作实现，覆盖注册版高级功能。

## 新增操作

| 操作 | TickFlow 接口 | 用途 |
|------|--------------|------|
| `level2` | `level2.trades.get` / `level2.orders.get` | Level-2 逐笔成交/委托队列 |
| `options` | `options.get` | 期权行情/希腊字母/隐含波动率 |
| `ticks` | `ticks.get` | 逐笔成交明细 |
| `moneyflow` | `moneyflow.get` | 资金流向(主力/散户净流入) |

## 代码变更
- 文件: `quant/data/sources/tickflow_source.py`
- 新增行数: ~700 行
- 复用权限感知故障转移 (`_batch_no_perm`)、代码转换、日期格式化
- 批量请求分片 (quotes 5只/次、level2/options/ticks 单只/次)
- 统一错误处理、日志记录

## 验证
- 语法检查: ✅
- 全量测试: 546 passed
- 语法检查: ✅

## 下一步
→ Phase 1.4: BaostockSource 深化