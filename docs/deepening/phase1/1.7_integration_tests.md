# Phase 1.7 完成归档 — 集成测试

## 变更摘要
新增 `test/test_data_sources_integration.py`，提供 15 个端到端集成测试，覆盖数据源注册表、Fetch 统一入口、统一错误码、注册表自动发现/热重载 4 大核心功能。

## 测试覆盖

| 测试类 | 测试用例 | 覆盖功能 |
|--------|----------|----------|
| `TestDataSourceRegistry` | 5 | 单例、自动发现、配置加载、优先级排序、类型过滤 |
| `TestDataSourceFetch` | 4 | Fetch 统一入口、不支持操作、回退链、回退禁用 |
| `TestDataSourceErrorCode` | 3 | 可重试/致命/需回退错误码决策 |
| `TestRegistryAutoDiscovery` | 2 | 内置源注册、手动注册 |
| `TestRegistryHotReload` | 2 | 热重载无变化、关闭所有资源 |

## 关键验证点

| 功能 | 验证内容 | 结果 |
|------|----------|------|
| 单例模式 | `get_registry()` 返回同一实例 | ✅ |
| 自动发现 | 6 大核心源全部注册 | ✅ |
| Fetch 统一入口 | 不支持操作返回 `UNSUPPORTED_OPERATION` | ✅ |
| 致命错误处理 | `UNSUPPORTED_OPERATION` 立即返回不重试 | ✅ |
| 回退链 | Baostock 失败 → Akshare 成功 | ✅ |
| 回退禁用 | `fallback_sources=[]` 时不回退 | ✅ |
| 错误码决策 | `is_retryable`/`is_fatal`/`requires_fallback` | ✅ |
| 自动发现 | 6 大核心源 + 手动注册 | ✅ |
| 热重载 | 无变化返回 False、关闭资源 | ✅ |

## 代码变更
- 新增文件: `test/test_data_sources_integration.py` (15 测试用例)
- 修复文件: `quant/data/sources/base.py` (致命错误立即返回)、`quant/data/sources/registry.py` (热重载逻辑)
- 测试通过率: 15/15 ✅
- 全量回归: 561 passed (546 原有 + 15 新增) ✅

## 遗留问题
- Prometheus metrics 重复注册警告 (非阻断, 需后续优化)
- config.yaml 缺少 `data.sources` 详细配置 (需后续补全)

## 下一步
→ Phase 2: Dagster 编排深化