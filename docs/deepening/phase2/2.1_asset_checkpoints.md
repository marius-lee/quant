# Phase 2.1 完成归档 — Asset 检查点

## 变更摘要
为所有 15 个 Dagster Asset 添加 `context.add_output_metadata()` 调用，在 Dagster UI 中展示完整的物化信息。

## 新增元数据字段

| Asset | 新增元数据字段 |
|-------|----------------|
| `daily_repair` | `duration_ms`, `partition_date`, `status` |
| `factor_cache_distributed` | `duration_ms`, `partition_date`, `status`, `summary` |
| `signals` | `duration_ms`, `partition_date`, `targets`, `elapsed`, `status` |
| `execute` | `duration_ms`, `partition_date`, `sells`, `limit_buys`, `status` |
| `snapshot_open` | `duration_ms`, `partition_date`, `saved`, `errors`, `status` |
| `monitor` | `partition_date`, `status` |
| `snapshot_close` | `duration_ms`, `partition_date`, `saved`, `errors`, `status` |
| `reconcile` | `duration_ms`, `partition_date`, `recon_status`, `status` |
| `daily_data` | `duration_ms`, `partition_date`, `status` |
| `adj_factor` | `duration_ms`, `partition_date`, `rows`, `status` |
| `factor_cache` | `duration_ms`, `partition_date`, `status` |
| `attribution` | `duration_ms`, `partition_date`, `status` |
| `lgb_train` | `duration_ms`, `partition_date`, `status` |
| `xgb_train` | `duration_ms`, `partition_date`, `status` |
| `weekly_eval` | `duration_ms`, `partition_date`, `status` |

## 代码变更
- 文件: `quant/orchestrator/dagster_assets.py`
- 所有 15 个 Asset 均添加 `start_time = time.perf_counter()` + `context.add_output_metadata()`
- 统一使用 `time.perf_counter()` 高精度计时

## 验证
- 语法检查: ✅
- 全量测试: 561 passed
- Dagster Definitions 加载: ✅ (15 Assets, 2 Jobs, 2 Schedules, 1 Sensor, 4 Resources)

## Dagster UI 效果
物化完成后，Dagster Asset Details 面板将显示：
- **Duration**: 物化耗时 (ms)
- **Rows/Targets/Sells/Buys/Saved**: 业务关键指标
- **Partition Date**: 分区日期
- **Status**: completed/skipped

## 下一步
→ Phase 2.2: 分区动态生成