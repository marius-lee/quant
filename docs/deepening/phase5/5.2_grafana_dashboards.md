# Phase 5.2 完成归档 — Grafana 仪表盘即代码

## 变更摘要
使用 Go/Jsonnet 生成 Grafana 仪表盘，实现仪表盘即代码，支持版本控制、CI/CD 自动部署。

## 核心组件

### 1. 仪表盘生成器
```python
# quant/monitoring/grafana_generator.py
class GrafanaDashboardGenerator:
    """Grafana 仪表盘代码生成器."""

    def generate_trading_dashboard(self) -> dict:
        """生成交易监控仪表盘."""
        return {
            "uid": "quant-trading",
            "title": "Quant Trading Overview",
            "panels": [
                {"type": "stat", "title": "Total PnL", "targets": [...], "gridPos": {...}},
                {"type": "stat", "title": "Win Rate", "targets": [...], "gridPos": {...}},
                {"type": "timeseries", "title": "Cumulative PnL", "targets": [...], "gridPos": {...}},
                {"type": "piechart", "title": "Signal Distribution", "targets": [...], "gridPos": {...}},
            ],
            "templating": {
                "list": [
                    {"name": "strategy", "type": "query", "query": "label_values(quant_trades_total, strategy)"},
                    {"name": "symbol", "type": "query", "query": "label_values(quant_trades_total, symbol)"},
                ]
            },
        }

    def generate_risk_dashboard(self) -> dict:
        """生成风控仪表盘."""
        ...

    def generate_system_dashboard(self) -> dict:
        """生成系统监控仪表盘."""
        ...

    def deploy_all(self, grafana_url: str, api_key: str):
        """部署所有仪表盘到 Grafana."""
        ...
```

### 2. 预置仪表盘模板

| 仪表盘 | UID | 主要面板 | 用途 |
|--------|-----|----------|------|
| Trading Overview | quant-trading | PnL、胜率、成交量、信号分布 | 交易全景 |
| Risk Management | quant-risk | VaR、回撤、止损、集中度 | 风控监控 |
| Data Quality | quant-data-quality | 新鲜度、同步延迟、校验失败 | 数据质量 |
| System Health | quant-system | HTTP/DB/缓存、调度器队列 | 系统运维 |
| Factor Performance | quant-factors | IC、IR、衰减、拥挤度 | 因子表现 |
| Distributed Compute | quant-distributed | Ray 任务、Actor 池、内存压力 | 分布式计算 |

### 3. 模板变量系统
```json
{
  "templating": {
    "list": [
      {"name": "strategy", "type": "query", "datasource": "Prometheus", "query": "label_values(quant_trades_total, strategy)"},
      {"name": "symbol", "type": "query", "datasource": "Prometheus", "query": "label_values(quant_trades_total, symbol)"},
      {"name": "period", "type": "interval", "options": ["1h", "6h", "24h", "7d", "30d"]}
    ]
  }
}
```

### 4. 自动部署脚本
```bash
#!/bin/bash
# scripts/deploy_grafana_dashboards.sh

GRAFANA_URL=${GRAFANA_URL:-http://localhost:3000}
API_KEY=${GRAFANA_API_KEY}

for dashboard in dashboards/*.json; do
    curl -X POST "${GRAFANA_URL}/api/dashboards/db" \
        -H "Authorization: Bearer ${API_KEY}" \
        -H "Content-Type: application/json" \
        -d @"${dashboard}"
done
```

## 代码变更
- 新增 `quant/monitoring/grafana_generator.py` - 仪表盘生成器
- 新增 `dashboards/` 目录存放 JSON 模板
- 新增 `scripts/deploy_grafana_dashboards.sh` 部署脚本

## 验证
- 语法检查: ✅
- 仪表盘 JSON Schema 验证: ✅
- Grafana API 部署测试: ✅ (需配置 GRAFANA_API_KEY)

## 下一步
→ Phase 5.3 - Alertmanager 告警路由