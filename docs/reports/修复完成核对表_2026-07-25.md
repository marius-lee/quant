# 修复完成核对表（2026-07-25）

> 基准文档: [项目全面分析报告_2026-07-24.md](./项目全面分析报告_2026-07-24.md)
> 状态图例: ✅ 已修复并验证 / 🟡 部分完成 / ⬜ 未做(含原因) / ⊘ 当前代码不存在(先前版本已修)
> 验证手段: `bash scripts/smoke_verify.sh` (12 段全绿) + `pytest test/` (71 passed) + 生产库副本迁移实测。

## 一、Bug 清单（报告 §4）

### P0 — 生产已失效/正在发生

| 条目 | 状态 | 修复内容 |
|---|---|---|
| B-01 盘中止损止盈完全失效 | ✅ | `stop_loss._compute_atr` 数据源 `DatabaseManager.trades()` → `market()` |
| B-02 归因任务每天崩溃 | ✅ | `attribution._require_cfg` 改模块级 import，删局部 import |
| B-03 execute 成功路径每天 RuntimeError | ✅ | 删 try 内 `_tk_finish`，finally 统一 finish 一次 |
| B-04 BacktestEngine 不可达 | ✅ | 类定义 dedent 到模块级 |
| B-05 daily_equity 链路坏死 | ✅ | 加 strategy 列+PK(date,strategy)；新增 meta KV 表（set/get/clear_flag）；生产库已迁移生效 |

### P1 — 正确性

| 条目 | 状态 | 修复内容 |
|---|---|---|
| B-06 权益不按市值计价 | ✅ | `get_capital(strategy, prices=None)` 支持 MTM；`broker.get_mtm_capital()`；回测净值三处、sizing、归因、基准跟踪全链路 MTM |
| B-07 基准点位当收益率 | ✅（先前已修） | `_bm_levels.pct_change().dropna()` 转换在册（loop.py:381） |
| B-08 价格复权口径混杂 | 🟡→✅(代码) | **v1**: tushare adj_factor 在线转 qfq、sina 移除。**v2（限流后重构）**: adj_factor 落本地表（`adj_factor` + `adj_factor_state`），`_fetch_batch_tushare` 读本地因子转 qfq 彻底绕开 1次/小时限流；`sync_adj_factor()` 后台批量填充；`_rebase_ex_dividend()` 除权日自动重写历史（×F_old/F_new，数学已验证）。**v3（test-v304，2026-07-26 数据源链审计）**: tickflow 日K 未复权 fallback 混写漏洞收口 — `_fetch_tickflow_daily` 复用同一 `_local_qfq_ratio` 转 qfq（无覆盖→None 交下一源）；附纠偏 `_source_speed`"动态轮转"误导注释、baostock py3.14 过时注释。**存量数据**: `scripts/resync_daily_qfq.py` 待因子表铺满后全量执行 |
| B-09 Series 真值判断 | ✅ | `is not None and len > 0` |
| B-10 ctx.store NameError | ✅ | ctx 分支只留 db_path/suppress_push |
| B-11 事件 B 死分支 | ✅ | 已删除 |
| B-12 capital/combine_mode 被忽略 | ✅ | `total_capital = capital if capital else seed`；`AlphaModel(combine_mode=combine_mode)` |
| B-13 裁剪公式误丢单 | ✅ | 边际成本 while 递减，`available -= o.cost` |
| B-14 熔断只写状态无人消费 | ✅ | monitor 写 DB flag（自愈清除），execute 消费 flag 阻断买单 |
| B-15 report.py 调不存在的方法 | ✅ | `repo.get_connection()` → `repo._conn()` |
| B-16 涨跌幅统一 10% 阈值 | ✅ | `_price_limit_pct()` 板块区分（68/30→20%，4/8/92→30%，主板 10%） |
| B-17 factor_cache 排除 active 因子 | ✅ | 物化池 backtesting ∪ using |
| B-18 sealed 重分配用昨收价 | ✅ | 改实时价 `prices.get(sym, tp["price"])` |
| B-19 主键只有 date 多策略覆盖 | ✅ | 三表全迁移：daily_equity PK(date,strategy)、daily_signals PK(date,strategy,mode)（附带修复 save_signals 忽略 mode 参数）、benchmark_tracking 加 strategy 列+PK(date,strategy)（含 rolling metrics/summary 全链路 strategy 过滤）。生产库已生效 |
| B-20 get_average_cost 假 FIFO | ✅ | 真 FIFO 逐笔撮合（冒烟验证：100+120 混合成本卖出后 = 120.0 ✓） |

### P2 — 小问题/坏味道

| 条目 | 状态 | 修复内容 |
|---|---|---|
| B-21 印花税率过时 | ✅ | 0.001 → 0.0005（2023-08 新政） |
| B-22 errors 计数器/`_adj_ic_map` 丢弃 | ✅ | errors 递增+单日容错；adjusted map 随 diag 返回 |
| B-23 orchestrator 超时形同虚设 | ✅ | `_TIMEOUTS` 全配置（1800~7200s）+ `_MAX_TASK_RETRIES=2` + 当日中止检测 |
| B-24 market_impact 硬编码 10 元 | ✅ | 加 `price` 参数 |
| B-25 DataStore 后台拉数线程 | ⊘ | 当前代码不存在 |
| B-26 update_daily 首批空数据不 break | ⊘ | 当前代码不存在 |
| B-27 state_broker 每次全量重建 | ✅ | 3s TTL 缓存+深拷贝防 `_quote_overlay` 污染 |
| B-28 /api/trade 无鉴权+明文 token | ✅ | token 移入 gitignored `quant/config/.env`（`${ENV}` 占位符机制）；`/api/trade` 支持 `QUANT_API_TOKEN` + X-API-Token 头（hmac 比较）。⚠️ 遗留：已泄露的旧 token 需到 tushare/tickflow 官网轮换 |

**Bug 汇总: 28 项 — 25 修复，2 不存在，1 代码修复待数据迁移（B-08 存量）**

## 二、逻辑错误与重构建议（报告 §7）

| # | 条目 | 状态 | 说明 |
|---|---|---|---|
| 1 | sizing 用初始本金 vs 当前权益口径矛盾 | ✅ | 统一为当日 MTM 权益（loop.py sizing 用 `get_capital(prices=当日收盘)`） |
| 2 | 两套止损并行+实盘无冷却 | 🟡 | 实盘已加当日冷却（execute.py `_stopped` 集合，止损标的从 targets 剔除）；统一 RiskManager 服务属架构重构，未做 |
| 3 | OOS 窗口太短（60d/10d） | ✅ | train 60→250、test 10→20、min_is_points 6→20、min_oos_points 2→5 |
| 4 | `abs(ic_ir)` 负 IC 因子信号反向 | ✅ | 四处权重计算全保留符号（stats_cache/ic.py/bridge.py/analyze.py），诊断 clamp 同步修 |
| 5 | 关键路径裸 except 吞错 | ✅ | 5 处改日志可观测（pipeline VaR、loop stress test、stats_cache 加载、phase5/7）；import 回退、log flush 等合理容错保留 |

## 三、业务操作流程（报告 §1）

| # | 条目 | 状态 |
|---|---|---|
| 1 | 两套权益口径并存 | ✅（B-06 全链路 MTM） |
| 2 | 回测/实盘撮合语义分裂 | ✅（B-12 参数修复 + ExecutionModel 统一抽象落地，见下） |
| 3 | 数据复权口径混杂 | 🟡（B-08 代码完成，存量数据迁移排期中） |
| 4 | rebalance_freq 配置无人读 | ✅ 实现周频调仓（2026-07-26）：`is_rebalance_day()`（calendar.py，daily 恒 True / weekly=本周 anchor 起首个交易日，假期顺延）；ExecutionModel.run(risk_only=True) 非调仓日只跑硬止损；接线 execute.py（实盘，非调仓日不要求信号）+ loop.py（回测，非调仓日跳过 generate_signals 省 ~80% 计算）+ broker.execute_risk_only；min_holding_days 已删（周频隐含最小持有），config 加 rebalance_weekday；测试 test_execution_model.py +3 项 |
| 5 | 风控只有告警没有控制 | ✅（B-01 止损恢复、B-14 熔断落单） |
| 6 | OMS 无对账闭环 | ✅（2026-07-26）：scheduler/reconcile.py 每日 15:05 三账核对——①持仓 vs 当日信号（非调仓日记 skip）②现金：不变量 cash≥0 + 跨源 equity_cross（最近 daily_equity 快照+(快照日,今日]全部流水；注：全账本重算与 get_cash 同源=循环论证，已弃用）③订单审计：状态/原因分组 + 残留 pending break + filled_but_no_trade 跨源核对。落 daily_recon 表（幂等），break→metrics+日志；orchestrator/status/超时/分组已注册，config recon.cash_drift_tolerance=1.0；web /api/recon + 调度 Tab 对账区块；测试 test_reconcile.py 9 项。顺手修 B-29：order_manager._fill 引擎跳单（除权）时仍标 filled → 改 cancel+note。**首次实战即立功**：抓出旧版 smoke_verify.sh 直写生产库的假 daily_equity 种子（cash=4500 整百值，会污染 get_max_drawdown→熔断）→ 已清 2 行；equity_cross 公式同步加固（快照滞后防误报）；当前版 smoke 已用 tmp 库无污染 |

## 四、技术选型（报告 §3）

| # | 条目 | 状态 |
|---|---|---|
| 1 | SQLite→DuckDB+Parquet | ⬜ 未做（观察 B-23 修复后锁冲突是否缓解再定） |
| 2 | 自研编排器→APScheduler/systemd | ⬜ 未做（B-23 超时+重试已大幅缓解痛点） |
| 3 | adj_factor 复权因子表 | ✅（B-08 v2 落地，含 rebase 机制） |
| 4 | polars/numba 加速 | ⬜ 未做 |
| 5 | 告警通道为空 | ⬜ 未做（需用户提供 bark/企业微信 webhook） |
| 6 | 明文 token | ✅；测试覆盖率/CI ⬜ 未做 |

## 五、架构优化建议（报告 §6）

| # | 条目 | 状态 |
|---|---|---|
| 1 | 统一执行抽象 ExecutionModel | ✅（quant/execution/execution_model.py：Template Method 共用链=冷却过滤→硬止损→delta→validate+alpha裁剪→分单成交；Backtest 买卖市价立即成交 / Live 卖市价+买限价+熔断；pipeline.execute_signals 与 scheduler/execute 双接线；行为变化=回测 validate 失败从全丢改为按 alpha 裁剪，与实盘统一；test/test_execution_model.py 5 项） |
| 2 | 估值层（MTM 写入 daily_equity） | ✅（B-06 + attribution 每日落表） |
| 3 | 数据层切分 | ✅ Phase 1（2026-07-26）：盘点 9 库=4 活跃+1 监控+4 僵尸（sim_trades/quant/factor 0B、benchmark 空表）→ 删 4 僵尸文件+死代码（QUANT_DB/BENCHMARK_DB 常量、DatabaseManager.benchmark()）；paths.py 按两域分组+owner 声明（账务域 trades/backtest_trades→TradeRepo；行情因子域 market/factor_cache→DataStore/FactorStore；监控域 metrics→monitor/metrics）；跨域只读收在 repo/store 层（attribution: trades+market、bridge: factor_cache+market 合法）。Phase 2=DuckDB/Parquet 迁移属 §4.1，待 B-08 数据迁移完+锁观察期 |
| 4 | scheduler/evaluation 职责合并 | ✅（2026-07-26）：phase5_monitor.py 484→240 行——删 run_monitor（仅 --phase5 触发且 _check_turnover 拿 market conn 查 sim_trades 必崩）+ crowding/turnover/capacity 三个与 attribution 重复实现；保留 sync_factor_status（手工管线终段活代码）；_check_ic_decay 活化到 weekly_eval（复用 force_refresh_cache 的 stats["decay"]，retention<30% 告警+metrics，零额外计算）；eval_standard.sh 删 --phase5 块；边界注释：attribution=日频实盘组合监控，evaluation=周频/手工因子生命周期 |
| 5 | 配置治理 | ✅（CIRCUIT_BREAKER_PCT 硬编码已入 config；rebalance_freq 已实现+rebalance_weekday 入 config+loader 校验） |

## 六、算法评估（报告 §8）

| # | 条目 | 状态 |
|---|---|---|
| 1 | 先修口径（MTM/复权/benchmark） | ✅（B-06/07/08 代码全修） |
| 2 | 接线 HRP/Regime/Optuna | ✅（2026-07-26, test-v299）：`optimizer.method` 死配置→真实分发（hrp 新默认/risk_parity 可选，仅 small 层有协方差时生效，Nano 实盘行为不变）；`alpha.regime_combine: true` 接线 `combine_regime()`，实盘 pickle HMM、回测 loop.py point-in-time 注入（起始日前训练+截止当日前向滤波，防前视），`regime.train_start` 入 config；run_task.sh 加 `hyperopt [trials]` 入口（test-v298 修复后闭环）；附带修 detector.py/deflated_sharpe.py 模块级缺 `_require_cfg` import 的潜伏 NameError；测试 +15 项，smoke 回测确认 regime combine 真实触发 |
| 3 | 交易成本感知优化（α-λ·TC） | ✅（2026-07-26, test-v297）：construct() 新增 current_lots+cost_model，各层理想目标统一过 Grinold 无交易区间——E[Δr]=Δz×IC_eff×σ×horizon×金额 ≥ λ×实际成本（CostModel 实算含 ¥5 最低佣金）才换仓；z=Blom 分位逆累积，IC_eff 优先运行时 ic_map |IC| 均值；只拦"以卖养买"配对，纯加仓/跌出候选卖出不设槛；generate_signals 从 engine 读持仓接线，回测/实盘双侧生效；config optimizer.tc_lambda/tc_horizon_days/tc_ic_ref（0.015 按 registry 实测校准）；daily_signals.reason 标 tc_hold；测试 +11 项，真实回测确认拦截触发 |
| 4 | CPCV+DSR 驱动替代短窗判定 | ✅（2026-07-26, test-v300）：新模块 `evaluation/cpcv_dsr.py`——factor_ic_daily live 序列 → Purged CPCV 拼 OOS 段 → 日频 ICIR → DSR 多重检验校正（M=评估因子总数，偏度/峰度实测）；attribution L2 由单切分 OOS/IS 比率（250/20）换为 verdict 驱动（degraded DSR<0.5 / significant ≥0.95 / insufficient <40 天不罚）；D1/D2/D3 reason 改记 DSR；死键 oos_warning_decay/oos_recovery_threshold 删除，新增 attribution.dsr_*/cpcv_* 四键；单位坑记录：DSR 公式 per-period 口径，observed SR 传年化会膨胀 √244 倍；测试 +13 项 |
| 5 | ML 合成（LightGBM ranker） | ⬜ 门禁未过不启动 |

## 七、报告后新增修复（2026-07-25 当日追加）

| 条目 | 说明 |
|---|---|
| strategy_config 新库迁移崩溃 | 全新 DB 初始化时 `no such column: initialized`（生产旧表有该列所以未暴露）→ 迁移按列存在性分支 |
| adj_factor 限流重构（B-08 v2） | 本地因子表+后台填充+自动 rebase，彻底绕开 1次/小时限流 |
| 运维工具链 | `scripts/smoke_verify.sh`（12 段冒烟）、`scripts/resync_daily_qfq.py --factors-only`、`scripts/add_adj_factor_cron.sh`（幂等装 cron）、`run_task.sh adj_factor` 任务、setup_cron.sh hourly 条目（已安装：每小时 :50） |
| 日频业务闭环审计（2026-07-26, test-v301） | 4 断点全修：① reconcile 史上零执行 → run_task.sh case + crontab 15:05 条目；② cron+daemon 双调度 grace≪实际时长误 abort 活任务（factor_cache 145s 被杀→僵尸持锁→daily_data locked）→ grace 全面对齐 _TIMEOUTS + monitor rid=None 守卫；③ attribution G1 窗口冲突（v296 裁剪 244cd < oos_verify 需求 275cd 永久缺口）→ train_window 250→170；④ G4 排程倒挂（attribution 20:00 早于 factor_cache 21:00 物化 → 每交易日必崩）→ cache miss 改 warning+跳过。attribution 周日实跑全链路 OK |
| 晚间调度重排（2026-07-26, test-v302） | 断点 3/4 根因=固定时刻表拓扑错（attribution 恒在因子物化前跑，G1/G4 需当日缓存）→ cron 三条（19/20/21 点）并成 19:00 `evening_chain` 单入口依赖链：daily_data → factor_cache → attribution，ok 门控+失败 fail-loud+各阶段独立 task_runs；orchestrator daemon 同步改 ok 门+顺序对调（成兜底）；新模块 scheduler/evening.py，测试 +8 项 |

## 八、待办（按时间顺序）

1. **进行中**: adj_factor 因子表后台填充（每小时 50 股，~4.6 天铺满 5525 股，cron 已装）。查进度: `sqlite3 quant/data/market.db "SELECT COUNT(DISTINCT symbol) FROM adj_factor"`
2. **铺满后**: 全量重拉存量数据 `PYTHONPATH=. .venv/bin/python3 scripts/resync_daily_qfq.py`（只走 daily 接口，30-60 分钟）
3. **尽快**: tushare/tickflow 官网轮换已泄露 token（B-28 遗留）
4. ~~决策: rebalance_freq~~ ✅ 已实现周频调仓（2026-07-26）
5. ~~ExecutionModel 统一抽象~~ ✅ 已完成（2026-07-25）
6. ~~OMS 对账闭环（§1.6）~~ ✅ 已完成（2026-07-26）
7. ~~rebalance_freq 周频调仓（§1.4）~~ ✅ 已完成（2026-07-26）
8. ~~数据层切分 Phase 1（§6.3）/ 职责合并（§6.4）~~ ✅ 已完成（2026-07-26）
9. ~~hyperopt 空转 bug~~ ✅ 已修复（2026-07-26, test-v298）：`loader.override()` 上下文管理器直接覆盖 config 单例（key 必须已存在，fail-fast）；`run_backtest(combine_mode=)` 新参数打通死维度；搜索空间清理（lookback_days 60-365 被 378 clamp → 400-800；删 Nano 无用的 max_single_position）；OPTUNA_* env 全删。测试 +8 项
10. ~~交易成本感知组合优化（§8.3, α-λ·TC）~~ ✅ 已完成（2026-07-26, test-v297）
11. ~~接线 HRP/Regime/Optuna（§8.2）~~ ✅ 已完成（2026-07-26, test-v299）
12. ~~CPCV+DSR 驱动替代短窗判定（§8.4）~~ ✅ 已完成（2026-07-26, test-v300）
13. **下一优先级开发项**: 数据层 Phase 2（§4.1 DuckDB/Parquet，待 B-08 存量迁移 + 锁观察期）
