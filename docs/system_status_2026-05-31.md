# 系统状态报告 — 2026-05-31

**项目**: A股量化选股系统 (QuantA)
**审计人**: 系统架构师 (Claude)
**最后更新**: 2026-05-31 (全面修复后)

---

## 当前状态

| 维度 | 评估 | 说明 |
|------|------|------|
| 功能完整性 | ⭐⭐⭐⭐ | 全链路（数据→因子→模型→回测→Web）可用 |
| 代码质量 | ⭐⭐⭐ | 全面修复后无致命/高优缺陷 |
| 测试覆盖 | ⭐⭐ | 43个单元测试覆盖核心模块，端到端测试待加 |
| 数据可靠性 | ⭐⭐⭐⭐ | 前视偏差已消除，增量更新健壮 |
| 性能 | ⭐⭐⭐⭐ | 向量化回测秒出，内存峰值可控 |
| 可维护性 | ⭐⭐⭐ | 配置激活，日期统一，错误可观测 |
| 安全性 | ⭐⭐⭐ | Token 已移除，无 SQL 注入 |

## 已验证指标

| 指标 | 值 | 备注 |
|------|-----|------|
| 股票覆盖 | ~5500 (全A股) | |
| 日线数据 | ~3700只有数据, 63万行 | market.db |
| 因子总数 | 63-83 (手工+自动) | 含真实基本面8个+auto-alpha 20个 |
| 集成模型 | 5 (LGBM+XGB+RF+ET+Ridge) | |
| Web 端口 | 8521 | Flask |
| 定时任务 | 每天 8:00 + 16:00 | launchd |
| 数据源 | tushare + akshare + 腾讯财经 | |
| 回测 | 向量化 + 按月再平衡 | |

## 技术栈

```
语言:     Python 3.14
数据库:   SQLite (WAL mode, market.db + results.db)
因子:     NumPy + pandas
模型:     scikit-learn + LightGBM + XGBoost
回测:     pandas 向量化
Web:      Flask + ECharts
调度:     macOS launchd
```

## 已知限制

| 限制 | 严重度 | 计划 |
|------|--------|------|
| 无幸存者偏差校正 | 中 | 下阶段 |
| 无企业行动处理(分红/拆股) | 中 | 下阶段 |
| 无涨跌停限制 | 低 | 事件引擎已实现,未启用 |
| 单次7:3拆分，无滚动验证 | 中 | 改进架子已有 |
| 因子注册无中心表 | 低 | 架构性决策 |

## 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    Web UI (Flask :8521)                  │
│                /api/run  /api/latest  /api/kline         │
├─────────────────────────────────────────────────────────┤
│              RecommendationEngine (pipeline.py)          │
│  ┌─────────┬─────────┬──────────┬──────────┬─────────┐ │
│  │ loader  │screener │ trainer  │predictor │ ranker  │ │
│  │ stocks  │IC筛选    │504天窗口 │分批预测   │demon    │ │
│  │+close   │+日期拆分 │5模型集成  │1天因子   │+中性化  │ │
│  └─────────┴─────────┴──────────┴──────────┴─────────┘ │
│  ┌──────────────────────────────────────────────────┐   │
│  │  backtest_runner (静态) / rebalance (按月再平衡)  │   │
│  └──────────────────────────────────────────────────┘   │
│  ┌──────────┐                                           │
│  │ builder  │ → results.db (runs + picks)               │
│  └──────────┘                                           │
├─────────────────────────────────────────────────────────┤
│  factor/           strategy/         backtest/          │
│  63手工+100自动     EnsembleModel     metrics + event    │
├─────────────────────────────────────────────────────────┤
│  data/                                                  │
│  DataStore → market.db (stocks + daily + meta)          │
│  FundamentalRepo / FactorRepo / PriceRepo               │
├─────────────────────────────────────────────────────────┤
│  config.yaml  +  loader.py                              │
└─────────────────────────────────────────────────────────┘
```

## 下次审计建议时间

- 新增因子后：重新验证 IC 筛选无前视偏差
- 数据量超过 1GB 后：评估 SQLite 是否需要升级 DuckDB
- 实际资金交易前：补充幸存者偏差校正 + 企业行动处理
